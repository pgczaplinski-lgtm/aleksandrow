/**
 * Widok: gratulacje po ukończeniu planu. (WŁASNOŚĆ: widoki uczestnika)
 * render(root, ctx): „Brawo!" + powrót do strony głównej.
 */

export function render(root, ctx) {
  throw new Error('not implemented');
}
