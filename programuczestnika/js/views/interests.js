/**
 * Widok: wybór zainteresowań. (WŁASNOŚĆ: widoki uczestnika)
 * render(root, ctx): karty tagów INTERESTS, zapis profilu przy każdym dotknięciu.
 */

export function render(root, ctx) {
  throw new Error('not implemented');
}
