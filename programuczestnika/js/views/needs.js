/**
 * Widok: wybór potrzeb rozwojowych. (WŁASNOŚĆ: widoki uczestnika)
 * render(root, ctx): karty tagów NEEDS, zapis profilu przy każdym dotknięciu.
 */

export function render(root, ctx) {
  throw new Error('not implemented');
}
