/**
 * Widok: plan dnia. (WŁASNOŚĆ: widoki uczestnika)
 * render(root, ctx): lista zajęć z godzinami, odhaczanie „Zrobiłem", wydruk.
 */

export function render(root, ctx) {
  throw new Error('not implemented');
}
