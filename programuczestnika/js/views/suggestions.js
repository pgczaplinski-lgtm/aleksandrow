/**
 * Widok: pomysły na dziś. (WŁASNOŚĆ: widoki uczestnika)
 * render(root, ctx): dopasowane zajęcia (store.matchActivities), dodawanie do planu.
 */

export function render(root, ctx) {
  throw new Error('not implemented');
}
